name = "bandar-financial-dashboard"
main = "worker.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# KV namespace for caching (will be created via Cloudflare dashboard or wrangler)
# [[kv_namespaces]]
# binding = "CACHE"
# id = "PLACEHOLDER_KV_ID"        # Replace after creating KV namespace
# preview_id = "PLACEHOLDER_KV_PREVIEW_ID"

# Environment variables (non-secret)
[vars]
NOTION_DB_ID = "c494c61e-d7eb-41e1-b615-849b73350e7e"

# Secrets set via: wrangler secret put NOTION_TOKEN
# Secrets set via: wrangler secret put INVALIDATE_SECRET
